version: 2

orbs:
   nchore-engine: anchore/anchore-engine@1.1.0

jobs:
  test-output:
    docker:
    - image: circleci/python:2.7-stretch
    steps:
    - checkout
    - run: echo "a stringgg"

workflows:
  version: 2
  test:
    jobs:
    - test-output
