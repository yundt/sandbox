version: 2.1

jobs:
  test-output:
    docker:
    - image: circleci/python:2.7-stretch
    steps:
    - checkout
    - run: echo "a string with : in it"

workflows:
  test:
    jobs:
    - test-output
